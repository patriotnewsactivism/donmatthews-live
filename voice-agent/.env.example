# x.ai Realtime
XAI_API_KEY=
XAI_AGENT_ID=agent_BVrCNfKW2CpeZyH2

# Twilio telephony
# Public base URL where this service is reachable (used to build the TwiML Stream URL)
PUBLIC_BASE_URL=https://voice.yourhost.example
# Optional shared secret appended to the Media Stream wss URL; validated on upgrade
STREAM_TOKEN=

# Supabase persistence (same project used by donmatthews.live)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Owner access ("sudo") verification
OWNER_PHONE=+18328804970
ADMIN_PASSCODE=2269
MAX_VERIFY_ATTEMPTS=3

# Admin tools via GitHub API (fine-grained PAT). Leave blank to disable admin tools.
GITHUB_TOKEN=
GITHUB_USER=

# Optional fallback text the agent gives for donation/support questions
# when no donation_info row exists in the voice_memory table yet.
DONATION_INFO_TEXT=Support the independent journalism and civil rights work at donmatthews.live. Visit the sites wtpnews.org or civilrightshub.org for the current support and donation options, or use Don's contact page to reach him directly.

PORT=8080